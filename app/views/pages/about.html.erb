<section class="py-16 md:py-24">
  <div class="max-w-7xl mx-auto px-6">
    <div class="max-w-4xl mx-auto">
      <!-- Header -->
      <header class="mb-16 md:mb-20 animate-fadeInUp">
        <div class="flex items-end gap-4 mb-4">
          <span class="text-[10px] font-bold uppercase tracking-[0.25em] text-[var(--color-accent)] font-[var(--font-family-display)]">About</span>
          <div class="flex-1 h-[2px] bg-[var(--color-accent)] mb-0.5"></div>
        </div>
        <h1 class="text-5xl md:text-6xl font-[var(--font-family-display)] font-black uppercase leading-[0.9] tracking-tight text-[var(--color-primary)]">
          Score<span class="text-[var(--color-accent)]">Base</span>
        </h1>
        <p class="mt-6 text-lg md:text-xl text-[var(--color-text-muted)] font-medium max-w-xl leading-relaxed">
          Search public domain music scores from IMSLP, CPDL, and PDMX in one place.
        </p>
      </header>

      <!-- Stats Grid -->
      <div class="mb-16 md:mb-20 animate-fadeInUp delay-100">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
          <!-- Total Count -->
          <div class="col-span-2 md:col-span-1 mm-card p-6 md:p-8">
            <p class="text-3xl md:text-4xl font-[var(--font-family-display)] font-black text-[var(--color-accent)] mb-2">
              <%= number_with_delimiter(@total_count) %>
            </p>
            <p class="text-xs font-bold uppercase tracking-[0.15em] text-[var(--color-text-muted)]">Total Scores</p>
          </div>

          <!-- Source counts -->
          <% sources = [
            { key: 'imslp', label: 'IMSLP' },
            { key: 'cpdl', label: 'CPDL' },
            { key: 'pdmx', label: 'PDMX' }
          ] %>
          <% sources.each_with_index do |source, i| %>
            <div class="mm-card p-6 md:p-8 animate-fadeInUp" style="animation-delay: <%= 150 + i * 50 %>ms;">
              <p class="text-2xl md:text-3xl font-[var(--font-family-display)] font-black text-[var(--color-primary)] mb-2">
                <%= number_with_delimiter(@source_counts[source[:key]] || 0) %>
              </p>
              <span class="mm-badge-source mm-badge-<%= source[:key] %>"><%= source[:label] %></span>
            </div>
          <% end %>
        </div>
      </div>

      <!-- About -->
      <div class="mb-16 md:mb-20 animate-fadeInUp delay-200">
        <div class="mm-card p-8 md:p-12">
          <div class="relative pl-6 border-l-[3px] border-[var(--color-accent)]">
            <p class="text-xl md:text-2xl font-[var(--font-family-display)] text-[var(--color-primary)] leading-snug font-semibold">
              Why this exists
            </p>
            <p class="mt-4 text-base text-[var(--color-text-muted)] leading-relaxed">
              There's a lot of public domain sheet music out there—IMSLP, CPDL, PDMX, and others. But they all have different websites, different search systems, and finding what you need often means checking multiple places.
            </p>
            <p class="mt-3 text-base text-[var(--color-text-muted)] leading-relaxed">
              I got tired of jumping between sites, so I built ScoreBase to search them all at once. That's basically it.
            </p>
          </div>
        </div>
      </div>

      <!-- Sources -->
      <div class="animate-fadeInUp delay-300">
        <div class="flex items-center gap-4 mb-8">
          <h2 class="text-xs font-bold uppercase tracking-[0.15em] text-[var(--color-accent)] font-[var(--font-family-display)]">Sources</h2>
          <div class="flex-1 h-px bg-[var(--color-border-subtle)]"></div>
        </div>

        <div class="space-y-4">
          <!-- IMSLP -->
          <div class="mm-card p-6 md:p-8 flex flex-col md:flex-row md:items-start gap-4">
            <div class="flex items-center gap-3 md:w-40 shrink-0">
              <span class="mm-badge-source mm-badge-imslp">IMSLP</span>
              <span class="text-lg font-[var(--font-family-display)] font-black text-[var(--color-primary)]">
                <%= number_with_delimiter(@source_counts['imslp'] || 0) %>
              </span>
            </div>
            <div>
              <a href="https://imslp.org" target="_blank" rel="nofollow noopener" class="text-base font-[var(--font-family-display)] font-bold text-[var(--color-primary)] hover:text-[var(--color-accent)] transition-colors mb-1 inline-block">
                International Music Score Library Project
              </a>
              <p class="text-sm text-[var(--color-text-muted)] leading-relaxed">
                Also known as the Petrucci Music Library. The largest collection of public domain sheet music with over 800,000 scores from 27,000+ composers.
              </p>
            </div>
          </div>

          <!-- CPDL -->
          <div class="mm-card p-6 md:p-8 flex flex-col md:flex-row md:items-start gap-4">
            <div class="flex items-center gap-3 md:w-40 shrink-0">
              <span class="mm-badge-source mm-badge-cpdl">CPDL</span>
              <span class="text-lg font-[var(--font-family-display)] font-black text-[var(--color-primary)]">
                <%= number_with_delimiter(@source_counts['cpdl'] || 0) %>
              </span>
            </div>
            <div>
              <a href="https://www.cpdl.org" target="_blank" rel="nofollow noopener" class="text-base font-[var(--font-family-display)] font-bold text-[var(--color-primary)] hover:text-[var(--color-accent)] transition-colors mb-1 inline-block">
                Choral Public Domain Library
              </a>
              <p class="text-sm text-[var(--color-text-muted)] leading-relaxed">
                Also known as ChoralWiki. The largest site for free choral sheet music, with strong coverage of Renaissance and Baroque vocal works.
              </p>
            </div>
          </div>

          <!-- PDMX -->
          <div class="mm-card p-6 md:p-8 flex flex-col md:flex-row md:items-start gap-4">
            <div class="flex items-center gap-3 md:w-40 shrink-0">
              <span class="mm-badge-source mm-badge-pdmx">PDMX</span>
              <span class="text-lg font-[var(--font-family-display)] font-black text-[var(--color-primary)]">
                <%= number_with_delimiter(@source_counts['pdmx'] || 0) %>
              </span>
            </div>
            <div>
              <a href="https://github.com/pnlong/PDMX" target="_blank" rel="nofollow noopener" class="text-base font-[var(--font-family-display)] font-bold text-[var(--color-primary)] hover:text-[var(--color-accent)] transition-colors mb-1 inline-block">
                Public Domain MusicXML
              </a>
              <p class="text-sm text-[var(--color-text-muted)] leading-relaxed">
                A research dataset of 250K+ public domain MusicXML files collected from MuseScore. Useful for notation software like MuseScore, Sibelius, or Finale.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- CTA -->
      <div class="mt-16 text-center animate-fadeInUp delay-400">
        <%= link_to root_path, class: "mm-button mm-button-primary text-xs px-8 py-3" do %>
          Start Searching
        <% end %>
      </div>
    </div>
  </div>
</section>
