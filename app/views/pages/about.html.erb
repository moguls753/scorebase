<section class="py-24 md:py-36">
  <div class="max-w-4xl mx-auto px-6 md:px-12">
    <!-- Header -->
    <div class="text-center mb-20 md:mb-28 animate-fadeInUp">
      <h2 class="text-5xl md:text-6xl font-[var(--font-family-display)] mb-8 font-black uppercase tracking-tight text-[var(--color-primary)]">
        About ScoreBase
      </h2>
      <p class="text-xl md:text-2xl text-[var(--color-text-muted)] font-medium max-w-2xl mx-auto leading-relaxed">
        A unified search for public domain music scores
      </p>
    </div>

    <!-- Stats Cards -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8 mb-20 md:mb-28 animate-fadeInUp delay-100">
      <div class="mm-card p-6 md:p-8 text-center">
        <p class="text-3xl md:text-4xl font-black text-[var(--color-accent)] mb-2"><%= number_with_delimiter(@total_count) %></p>
        <p class="text-xs md:text-sm text-[var(--color-text-muted)] font-semibold uppercase tracking-wider">Total Scores</p>
      </div>
      <% %w[cpdl imslp pdmx].each do |source| %>
        <div class="mm-card p-6 md:p-8 text-center">
          <p class="text-3xl md:text-4xl font-black text-[var(--color-primary)] mb-2"><%= number_with_delimiter(@source_counts[source] || 0) %></p>
          <p class="text-xs md:text-sm text-[var(--color-text-muted)] font-semibold uppercase tracking-wider"><%= source.upcase %></p>
        </div>
      <% end %>
    </div>

    <!-- What is ScoreBase -->
    <div class="mm-card p-10 md:p-14 mb-12 md:mb-16 animate-fadeInUp delay-200">
      <p class="text-lg md:text-xl text-[var(--color-text-muted)] leading-relaxed text-center max-w-2xl mx-auto">
        Public domain music scores are spread across multiple archives—IMSLP, CPDL, PDMX—each with different interfaces. ScoreBase combines them into one search.
      </p>
    </div>

    <!-- Sources -->
    <div class="mm-card p-10 md:p-14 animate-fadeInUp delay-300">
      <h3 class="text-2xl md:text-3xl font-[var(--font-family-display)] font-bold text-[var(--color-primary)] mb-12 uppercase tracking-tight text-center">
        Our Sources
      </h3>
      <div class="space-y-10 md:space-y-12 max-w-2xl mx-auto">
        <div class="flex items-start gap-6">
          <span class="mm-badge-source mm-badge-imslp shrink-0 mt-1">IMSLP</span>
          <div>
            <p class="text-base md:text-lg text-[var(--color-text-muted)] leading-relaxed">
              The International Music Score Library Project. The largest collection of public domain music scores,
              with hundreds of thousands of works from classical composers.
            </p>
          </div>
        </div>
        <div class="flex items-start gap-6">
          <span class="mm-badge-source mm-badge-cpdl shrink-0 mt-1">CPDL</span>
          <div>
            <p class="text-base md:text-lg text-[var(--color-text-muted)] leading-relaxed">
              The Choral Public Domain Library. Focused on choral and vocal music, including sacred works,
              madrigals, and art songs.
            </p>
          </div>
        </div>
        <div class="flex items-start gap-6">
          <span class="mm-badge-source mm-badge-pdmx shrink-0 mt-1">PDMX</span>
          <div>
            <p class="text-base md:text-lg text-[var(--color-text-muted)] leading-relaxed">
              Public Domain MusicXML. A curated collection of scores in MusicXML format,
              ideal for digital music notation software.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Back link -->
    <div class="mt-16 md:mt-20 text-center animate-fadeInUp delay-400">
      <%= link_to "Back to Search", root_path, class: "mm-button mm-button-secondary text-xs px-8 py-3" %>
    </div>
  </div>
</section>
