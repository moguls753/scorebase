#!/usr/bin/env bash
set -euo pipefail

mkdir -p .kamal
cat > .kamal/secrets <<EOF
RAILS_MASTER_KEY=${RAILS_MASTER_KEY:?required}
KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD:?required}
APPSIGNAL_PUSH_API_KEY=${APPSIGNAL_PUSH_API_KEY:?required}
EOF

exec bin/kamal deploy "$@"
